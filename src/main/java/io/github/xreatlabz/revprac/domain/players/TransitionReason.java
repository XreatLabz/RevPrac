package io.github.xreatlabz.revprac.domain.players;

public enum TransitionReason {
    JOIN,
    QUEUE_JOIN,
    MATCH_START,
    SPECTATE,
    EDITOR_OPEN,
    RETURN_TO_LOBBY,
    QUIT,
    PLUGIN_DISABLE
}
